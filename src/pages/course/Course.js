import "./Course.css"
import {useFetch} from "../../hooks/useFetch";

const Course = () => {
     const {data} = useFetch("")

    return (
        <div>

        </div>
    );
};

export default Course;